version: '3'
services:
  logstash:
    container_name: logstash
    image: logstash:7.6.0
    volumes:
      - ./config/:/usr/share/logstash/config/
      - ./pipeline/:/usr/share/logstash/pipeline/
      - /Users/hzl/Desktop/hadoopcloud/hadoop-cloud-register/logs:/user/share/logs
    ports:
      - "5044:5044"
      - "9600:9600"
    environment:
          LS_JAVA_OPTS: "-Xmx512m -Xms512m"
    restart: always

