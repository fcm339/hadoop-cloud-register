server:
  port: 8888

management:
  port: 8889
  endpoints:
    web:
      exposure:
        include: '*'

hadoop:
  openMulti: false #是否开启多数据源
  hdfs:
    name-node: "hdfs://localhost:9000" #hdfs的nameNode节点ip
