#服务端口号
server:
  port: 8888

spring:
  application:
    name: hzl-register

#禁止eureka作为注册中心
eureka:
  client:
    enabled: false


management:
  port: 8889
  endpoints:
    web:
      exposure:
        include: '*'

hadoop:
  openMulti: false #是否开启多数据源
